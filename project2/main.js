// script.js

// Optional JS for further interactivity or animations
document.querySelector('.cta-button').addEventListener('mouseenter', () => {
  console.log("Hovering over button!");
});
functionalities
console.log("Layout loaded successfully.");
document.addEventListener('DOMContentLoaded', () => {
  const headerBox = document.querySelector('.header-box');
  headerBox.style.transition = 'box-shadow 1s ease-in-out';
  headerBox.style.boxShadow = '0 0 30px rgba(255, 255, 255, 0.9)';
});
